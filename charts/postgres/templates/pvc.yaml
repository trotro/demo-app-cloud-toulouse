apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-volume-claim
  labels:
    app: postgres
spec:
  #storageClassName: local-path
  #accessModes:
  #  - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi